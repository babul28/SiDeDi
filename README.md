# Sistem Informasi Deteksi Dini

## How to Use?
* Clone this `repo` to your local repo
* install package with command `composer install`
* set `.env` file with your configuration
* running migration `php artisan migrate`
* **Happy Coding!!**